# Copy to .env (gitignored) and fill in. Must point at a disposable clone of
# the production database, never at production itself — the test suite
# refuses to run unless the database name contains "thufirdev" as a
# safety check (see conftest.py:db_url).
TEST_DATABASE_URL=postgresql://user:password@host/thufirdev
