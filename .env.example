POSTGRES_PASSWORD=changeme
VITE_ELECTRIC_URL=http://localhost:3000
VITE_API_URL=http://localhost:3001

# WebAuthn / Passkey config
# RP_ID must match the domain the browser page is served from (no port)
# RP_ORIGIN must be the full origin of the browser page
# Dev defaults (localhost) are set automatically; only needed for production
RP_ID=thufir.majid.org
RP_ORIGIN=https://thufir.majid.org
